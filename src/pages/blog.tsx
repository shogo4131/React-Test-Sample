import type { NextPage } from 'next';
import Layout from '../components/Layout';

const Blog: NextPage = () => {
  return (
    <Layout title="Blog">
      <p>Blog Page</p>
    </Layout>
  );
};

export default Blog;

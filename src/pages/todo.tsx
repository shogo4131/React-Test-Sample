import type { NextPage } from 'next';
import Layout from '../components/Layout';

const Todo: NextPage = () => {
  return (
    <Layout title="Todo">
      <p>Todo Page</p>
    </Layout>
  );
};

export default Todo;
